import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // For Firebase Hosting with static export
  // output: 'export', // Uncomment for static export
  trailingSlash: true,
};

export default nextConfig;
